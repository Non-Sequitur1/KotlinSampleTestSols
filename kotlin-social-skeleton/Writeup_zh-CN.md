# Kotlin: 社交达人

> 难度评价：3/5

**照 spec 做！！！照 spec 做！！！照 spec 做！！！照 spec 做！！！**

流程你懂的。

1. 照 spec 定义，没什么好说的。
2. 同上。
3. 这部分需要熟悉 Kotlin 中的扩展函数定义原理和方式。不过总体实现方式不难，还是那套，照着spec写就完了。
4. 本题正确解法是在 SimpleUser 的定义中，向构造函数中添加 befriendingStrategy 参数默认值设为 standardStrategy，并将原本依照名字写的 befriend 检查替换成 befriendingStrategy 函数的返回值。只需修改几行，便可解之。
5. 压轴题。解答此题时，需要仔细阅读并分析题目要求，严格按照规范定义数据结构，有一定难度。难点主要集中于哈希表的原理与实现，同时需要熟悉双向链表的工作机制，以及二者在LinkedHashMap中所展现的关系。

> **解题技巧：** 实现 “remove” 删除方式时，可以通过哈希表中的桶快速定位所需的链表节点，并依此进行添加／删除。

本题虽然难度较大，但同时占分比例也属本试卷中最高。
6. 照spec 写，定义可以直接替换成OrderedMap。
7. **需要熟悉 Java 语言中的匿名函数功能。同时需要特别注意防止互斥死锁情况（deadlock）。**
解决方式如下：
－ **不能单纯获取并释放互斥锁，需要注意获取顺序。**
－ 我建议按照 hashCode 顺序进行获取释放，具体实现方式可以参考代码逻辑。

> 综合评价：难度适中，有一定挑战性。前半部分较易，可当做练习来写。
 
