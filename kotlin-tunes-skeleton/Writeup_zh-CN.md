# Kotlin: 小曲演奏家

> 难度评价：1/5


1. 第一题的 Notes，直接照 spec 来定义类型。
2. 同上，直接照 spec 写定义。
3. 这题更容易。可以直接调用 targetTune 的相关函数。不过需要注意限制 notes 的范围从 0 到 200，否则会抛异常。
4. 同上，直接调用相关函数。
5. 同上，直接调用相关函数，不过调用前需要包着 `withLock`。Composer 定义可以直接使用 forEach, map 来做。


> 综合评价：这卷总体难度极易，算是五份试卷当中最容易的那份。大部分题目都可以直接照着 spec 的指示来回答，轻松秒杀。做卷时强烈建议仔细阅读 spec，严格按照其所提供标准实现类型。